[package]
name = "wasm-zip"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
zip = { version = "8.6", default-features = false, features = [
    "deflate64",
    "deflate",
] }